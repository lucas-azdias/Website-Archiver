[project]
name = "website-archiver"
version = "0.1.0"
description = "A multithreaded website archiver"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "requests>=2.34.2",
    "rich>=15.0.0",
]
